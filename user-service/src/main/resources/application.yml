server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/micro_user_db
    username: root
    password: theboy$G$Kpc2
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
rating:
  service:
    baseUrl: http://RATING-SERVICE
hotel:
  service:
    baseUrl: http://HOTEL-SERVICE